import 'package:flutter/material.dart';

class AppColors {
  static const Color primary      = Color(0xFFa2d2ff); // xanh chủ đạo — header, nút, bubble gửi
  static const Color primaryLight = Color(0xFFbde0fe); // xanh nhạt — avatar, accent, nền phụ
  static const Color primaryDark  = Color(0xFF1a4a7a); // xanh đậm — chữ trên nền xanh
  static const Color accent       = Color(0xFFcdb4db); // tím — badge, icon active, tab bar
  static const Color bubbleSend   = Color(0xFFa2d2ff); // bubble tin nhắn mình gửi
  static const Color bubbleRecv   = Color(0xFFffc8dd); // bubble tin nhắn nhận
  static const Color background   = Color(0xFFf9f9f9); // nền toàn app
  static const Color textDark     = Color(0xFF1a4a7a); // chữ trên nền xanh
  static const Color textMuted    = Color(0xFF2a5a8a); // chữ phụ, hint
  static const Color pink         = Color(0xFFffafcc); // hồng — nút gửi, highlight
}